name: Git Star Boost 🚀⭐

on:
  push:
    branches:
      - main  # Roda no push para a branch 'main'
  schedule:
    - cron: '0 0 * * *'  # Roda todos os dias à meia-noite (formato cron)

jobs:
  boost:
    runs-on: windows-latest  # Usando o runner Windows mais recente disponível no GitHub Actions

    steps:
      # Passo 1: Checkout do repositório
      - name: Checkout repository
        uses: actions/checkout@v3

      # Passo 2: Executar o script PowerShell
      - name: Run Git Star Boost Script
        run: |
          # Rodar o script PowerShell diretamente
          powershell -ExecutionPolicy Bypass -File .\git_star_boost.ps1
